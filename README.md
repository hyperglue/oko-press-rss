# Kanał RSS dla OKO.press

## Instalacja na glitch.com
```
REPO="https://raw.githubusercontent.com/hyperglue/oko-press-rss/glitch" && wget ${REPO}/install.sh && wget ${REPO}/glitch.json && refresh
```
